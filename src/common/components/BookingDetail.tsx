import { ReactElement } from "react";

interface Props {
  id?: string;
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function BookingDetail(props: Props): ReactElement {
  return <div>BookingDetail</div>;
}
